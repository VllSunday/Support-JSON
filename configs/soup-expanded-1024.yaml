base: ./models/qwen3.5-4b
task: sft
modality: text
backend: transformers
experiment_name: support-json-expanded-v0.4-1024
data:
  train: ./data/expanded-v0.4/train-1024.chatml.jsonl
  format: chatml
  val_split: 0.0
  max_length: 2048
  train_on_responses_only: true
  train_on_prompt: false
training:
  epochs: 1
  lr: 0.0001
  batch_size: 1
  gradient_accumulation_steps: 8
  seed: 42
  data_seed: 42
  warmup_ratio: 0.05
  optimizer: adamw_torch
  quantization: none
  load_in_16bit: true
  gradient_checkpointing: true
  packing: false
  stream_layers: false
  lora:
    r: 16
    alpha: 32
    dropout: 0.0
    target_modules: all-linear
  logging_steps: 1
  save_steps: 100
  train_on_eot: true
  use_liger: true
output: ./checkpoints
